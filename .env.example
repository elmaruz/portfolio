# Google Calendar API Configuration
# See: https://console.cloud.google.com

# OAuth 2.0 Client ID (from Google Cloud Console)
GOOGLE_CLIENT_ID=

# OAuth 2.0 Client Secret (from Google Cloud Console)
GOOGLE_CLIENT_SECRET=

# Refresh Token (obtained from /api/calendar/setup - one-time setup)
GOOGLE_REFRESH_TOKEN=

# Calendar ID (usually your email address, or 'primary' for the main calendar)
GOOGLE_CALENDAR_ID=primary

# Base URL for OAuth redirects (optional, defaults to localhost:3000 in dev)
# NEXT_PUBLIC_BASE_URL=https://your-domain.com

# Feature Flags
# Set to 'true' to enable the booking section
NEXT_PUBLIC_ENABLE_BOOKING=false
